USE harshith;

CREATE TABLE mobile(id int,brand varchar(20),model varchar(10),price bigint,
created_at timestamp,created_by varchar(20),modified_at timestamp,modified_by varchar(20));

DESC mobile;

SELECT * FROM mobile;